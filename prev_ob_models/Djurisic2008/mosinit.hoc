load_file("nrngui.hoc")

{
xpanel("Simulation selector", 0)
xlabel("Reproduces supplementary figure 2")
xlabel("which shows normalized spike amplitude")
xlabel("during retrograde invasion of the tuft,")
xlabel("with active or passive membrane.")
xbutton("Passive tuft","{ load_file(\"init_spiketuft.hoc\")  doit() }")
xbutton("Active tuft","{ load_file(\"init_active.hoc\")  doit() }")
xpanel(92,109)
}

